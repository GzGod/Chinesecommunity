# 快速开始指南

## 第一步：安装依赖

项目已经配置好了所有必要的依赖，直接安装即可：

\`\`\`bash
cd web3/web3-community
npm install
\`\`\`

## 第二步：配置数据库

### 选项1：使用本地PostgreSQL

1. 安装PostgreSQL（如果还没有）
2. 创建数据库：
\`\`\`bash
createdb web3community
\`\`\`

3. 配置环境变量：
\`\`\`bash
cp .env.example .env
\`\`\`

编辑 `.env` 文件：
\`\`\`env
DATABASE_URL="postgresql://postgres:password@localhost:5432/web3community"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-random-secret-key-here"
\`\`\`

### 选项2：使用在线数据库（推荐新手）

使用 [Supabase](https://supabase.com) 或 [Neon](https://neon.tech) 的免费PostgreSQL：

1. 注册账号并创建项目
2. 复制数据库连接字符串
3. 粘贴到 `.env` 文件的 `DATABASE_URL`

## 第三步：初始化数据库

\`\`\`bash
# 生成Prisma客户端
npm run db:generate

# 运行数据库迁移（创建表结构）
npm run db:migrate
\`\`\`

## 第四步：创建管理员账号

\`\`\`bash
# 使用默认账号（admin@example.com / admin123）
npm run create-admin

# 或者自定义账号
npm run create-admin your@email.com yourpassword "Your Name"
\`\`\`

## 第五步：启动开发服务器

\`\`\`bash
npm run dev
\`\`\`

访问 http://localhost:3000

## 第六步：登录管理后台

1. 访问 http://localhost:3000/admin/login
2. 使用刚才创建的管理员账号登录
3. 开始创建项目和发布内容！

## 常用命令

\`\`\`bash
# 开发
npm run dev              # 启动开发服务器
npm run build            # 构建生产版本
npm run start            # 启动生产服务器

# 数据库
npm run db:generate      # 生成Prisma客户端
npm run db:migrate       # 运行数据库迁移
npm run db:studio        # 打开Prisma Studio（数据库可视化工具）

# 管理员
npm run create-admin     # 创建管理员账号
\`\`\`

## 下一步

- 📝 在管理后台创建第一个Web3项目
- 📱 发布第一条内容（推文/截图/活动）
- 🎨 自定义网站样式和配置
- 🚀 部署到Vercel或其他平台

## 遇到问题？

### 数据库连接失败
- 检查 `.env` 文件中的 `DATABASE_URL` 是否正确
- 确保PostgreSQL服务正在运行
- 检查数据库用户名和密码

### 登录失败
- 确保已经运行 `npm run create-admin` 创建管理员
- 检查邮箱和密码是否正确
- 查看控制台是否有错误信息

### 端口被占用
\`\`\`bash
# 使用其他端口启动
PORT=3001 npm run dev
\`\`\`

## 技术支持

- 查看 [README.md](./README.md) 了解完整文档
- 查看 [计划文档](../../.claude/plans/golden-inventing-quail.md) 了解详细设计
